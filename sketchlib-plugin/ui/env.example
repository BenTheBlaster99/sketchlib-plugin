# Copy to .env before npm run build, then: npm run build

# Production (beta):
VITE_API_URL=https://sketchup-store-api-main-cupply.laravel.cloud/api

# Local dev (LAN — sister's PC on same Wi‑Fi):
# VITE_API_URL=http://192.168.217.133:8000/api
