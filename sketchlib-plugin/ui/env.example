# Copy to .env before npm run build
# Use your Ubuntu PC's LAN IP so your sister's Windows PC can reach the API.
# Find IP: ip addr  or  hostname -I
#
# Example: both PCs on same Wi‑Fi, Laravel: php artisan serve --host=0.0.0.0
VITE_API_URL=http://192.168.1.42:8000/api
